/** Optional Web bundle marker for native local Memory. @module @deepseek-ai/dsh-web-memory */

/** Bundle marker plugin body; behavior comes from `cordis.patch.yml` rows. */
export function apply(): void {}
